{
  "name": "kaizen-v1",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight Telegram accountability companion bot",
  "main": "netlify/functions/telegram-webhook.js",
  "scripts": {
    "dev": "netlify dev",
    "start": "netlify dev",
    "deploy-check": "node scripts/deploy-check.js",
    "logs": "node -e \"console.log('Netlify: netlify logs:function telegram-webhook (after netlify link)'); console.log('Or: Site → Logs → Functions in Netlify UI.');\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8"
  }
}
