# Copy to .env for local Netlify dev. Set real values in Netlify UI for production.
# (Duplicate of .env.example for tooling that expects env.example)

TELEGRAM_BOT_TOKEN=your_telegram_bot_token
WEBHOOK_SECRET=your_webhook_secret
